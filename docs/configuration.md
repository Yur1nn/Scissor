# Configuration

## Overview
- All server settings are managed via a config file (TOML/YAML/JSON, to be decided).
- Includes map selection, world rules, PvP mode, plugin loading, etc.

## Example Settings
- Map file path
- Default PvP mode
- Allowed commands
- Plugin directory

## Expansion
- Add new config options as features are added.
- Document config schema changes here.
